# Copia este archivo a la raíz del repo consumidor como `sdd-kit.config.yaml` y ajusta valores.
# El bootstrap también puede generarlo si no existe (ver README).

locale: es

# Workflow activo (ruta relativa al root del repositorio)
workflowFile: kit/workflows/green-field.v1.yaml

# Carpeta donde viven los artefactos SDD generados
artifactsDir: docs/sdd

# Destino de skills de Cursor relativas al root del repositorio
skillsTargetDir: .cursor/skills

# Versión del kit (informativo; el bootstrap la escribe en `_INDEX.md`)
kitVersion: 1.0.0
