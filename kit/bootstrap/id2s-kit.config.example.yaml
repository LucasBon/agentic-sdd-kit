# Copia este archivo a la raíz del repo consumidor como `id2s-kit.config.yaml` y ajusta valores.
# El bootstrap también puede generarlo si no existe (ver README).

locale: es

# Workflow activo (opcional hasta que Sebastian lo defina con id2s-role-project-manager)
# workflowFile: kit/workflows/green-field.v1.yaml

# Carpeta donde viven los artefactos humanos ID2S
artifactsDir: docs/id2s

# Carpeta de documentos agent-ready (versión mínima para agentes)
agentReadyDir: agent-ready-docs/id2s

# Destino de skills de Cursor relativas al root del repositorio
skillsTargetDir: .cursor/skills

# Versión del kit (informativo; el bootstrap la escribe en `_INDEX.md`)
kitVersion: 2.0.0
